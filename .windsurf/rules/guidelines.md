# Windsurf Guidelines - learn-mongodb

- Respect TypeScript NodeNext ESM conventions (`.js` extension on relative imports).
- Verify Vitest test suite (`npm test`) passes before submitting diffs.
- Adhere to the ESR (Equality, Sort, Range) rule for MongoDB indexing.
