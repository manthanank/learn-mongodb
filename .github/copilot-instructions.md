# GitHub Copilot Custom Instructions - learn-mongodb

- This project is a TypeScript + Express 5 + Mongoose 8 curriculum and sandbox for MongoDB.
- Use ES Module syntax with `.js` extensions on imports.
- Maintain compatibility with both connected MongoDB instances and the offline `mockDb` fallback.
- Format code with 2-space indentation and single quotes.
